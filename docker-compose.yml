version: '3.9'
services:
  messenger:
    build: .
    restart: unless-stopped
    ports:
      - "3001:3001"
    environment:
      - PORT=3001
      - MAX_FILE_SIZE_MB=10
    mem_limit: 700m
    cpus: 1.5
